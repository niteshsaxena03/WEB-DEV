#JSX

const ErrorExample = () => {
  const count=0;
  
  const handleClick=()=>{
    count+=1;
  }

  return <div>
  <h1>{count}</h1>
  <button type="text" onClick={handleClick} className="btn">increase</button>
  </div>;
};

export default ErrorExample;
