BABEL-->it is a compilor which can convert modern js code into native js code and makes code simpler 

JSX->it allows us to write html inside js and vice versa

#USING VERSION 16.11.0

import react from "react";
import reactdom from "react-dom";

reactdom.render(<div>
  <h1>MY FAVORITE FOOD</h1>
  <ul>
    <li>shahi paneer</li>
    <li>pizza</li>
    <li>burger</li>
  </ul>
  </div>,
  document.getElementById("root"));
