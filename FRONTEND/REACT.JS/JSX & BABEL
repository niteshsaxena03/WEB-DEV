BABEL-->it is a compilor which can convert modern js code into native js code and makes code simpler 

JSX->its a method to use react

import react from "react";
import reactdom from "react-dom";

reactdom.render(<div>
   <h1>HELLO</h1>
   <p>this is a paragraph</p></div>,
   document.getElementById("root"));
